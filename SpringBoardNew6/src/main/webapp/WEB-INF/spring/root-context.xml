<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- Root Context: defines shared resources visible to all other web components -->
	
	<!-- dataSource : db 연결 정보 객체
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName"
			value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy" />
		<property name="url"
			value="jdbc:log4jdbc:mysql://localhost:3306/springdb" />
		<property name="username" value="root" />
		<property name="password" value="1234" />
	</bean>
	dataSource : db 연결 정보 객체 -->
	
	<!-- DB 연결정보 객체 (hikariCP) -->
	<bean id="hikariConfig"
	      class="com.zaxxer.hikari.HikariConfig">
	      <!-- <property name="driverClassName"
	                value="com.mysql.cj.jdbc.Driver"/>
	      <property name="jdbcUrl"
		  			value="jdbc:mysql://localhost:3306/jspdb"/> -->
 		  <property name="driverClassName"
	                value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy"/>
	      <property name="jdbcUrl"
		  			value="jdbc:log4jdbc:mysql://localhost:3306/springdb"/>
		  <property name="username"
		  			value="root"/>
		  <property name="password"
		  			value="1234"/>
	</bean>
	<!-- DB 연결정보 객체 (hikariCP) -->
	
	<!-- DB 연결정보 객체 (hikariCP) -->
	<!-- DataSource DB 연결정보 객체 -->
	<bean id="dataSource"
	      class="com.zaxxer.hikari.HikariDataSource"
	      destroy-method="close">
	      <constructor-arg ref="hikariConfig"/>
	</bean>
	<!-- DB 연결정보 객체 (hikariCP) -->
	
	<!-- SQLSessionFactory - DB 연결(Connection), SQL 실행, Mybatis, mapper 설정 -->
	<bean id="sqlSessionFactory"
	      class="org.mybatis.spring.SqlSessionFactoryBean">
	      <property name="dataSource" ref="dataSource"/>
	      <property name="configLocation"
	                value="classpath:/mybatis-config.xml"/>
	      <property name="mapperLocations"
	                value="classpath:/mappers/**/*Mapper.xml"/>
	</bean>
	<!-- SQLSessionFactory - DB 연결(Connection), SQL 실행, Mybatis, mapper 설정 -->
	
	<!-- SQLSessionTemple 객체 - 자동 DB 연결(Connection) / 종료, 
	                             트랜잭션 관리, 스레드 처리 안정성 높임,
	                             SQL 실행, Mybatis, mapper 설정 -->
	<bean id="sqlSession"
		  class="org.mybatis.spring.SqlSessionTemplate" 
		  destroy-method="close">
		  <constructor-arg name="sqlSessionFactory" 
		                   ref="sqlSessionFactory"/>
	</bean>	                                                          
	<!-- SQLSessionTemple 객체 - 자동 DB 연결(Connection) / 종료, 
                             트랜잭션 관리, 스레드 처리 안정성 높임,
                             SQL 실행, Mybatis, mapper 설정 -->
	
	<!-- 외부에서 생성된 객체(빈)를 등록 -->
	<context:component-scan base-package="com.itwillbs.persistence"/>
	<context:component-scan base-package="com.itwillbs.service"/>                             
</beans>
